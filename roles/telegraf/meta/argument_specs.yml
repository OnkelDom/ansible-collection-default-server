---
argument_specs:
  main:
    short_description: Entry point for the telegraf role.
    description:
    - Validate variables for the `telegraf` role.
    options:
      telegraf_manage_repository:
        description:
        - Controls `telegraf_manage_repository` for the `telegraf` role.
        required: false
        type: bool
      telegraf_manage_service:
        description:
        - Controls `telegraf_manage_service` for the `telegraf` role.
        required: false
        type: bool
      telegraf_service_enabled:
        description:
        - Controls `telegraf_service_enabled` for the `telegraf` role.
        required: false
        type: bool
      telegraf_service_state:
        description:
        - Controls `telegraf_service_state` for the `telegraf` role.
        required: false
        type: str
      telegraf_manage_firewall:
        description:
        - Controls `telegraf_manage_firewall` for the `telegraf` role.
        required: false
        type: bool
      telegraf_firewall_zone:
        description:
        - Controls `telegraf_firewall_zone` for the `telegraf` role.
        required: false
        type: str
      telegraf_manage_selinux:
        description:
        - Controls `telegraf_manage_selinux` for the `telegraf` role.
        required: false
        type: bool
      telegraf_firewall_ports:
        description:
        - Controls `telegraf_firewall_ports` for the `telegraf` role.
        required: false
        type: list
        elements: str
      telegraf_manage_main_config:
        description:
        - Controls whether the role manages `telegraf.conf`.
        required: false
        type: bool
      telegraf_manage_plugin_configs:
        description:
        - Controls whether the role manages files in `telegraf.d`.
        required: false
        type: bool
      telegraf_manage_main_config:
        description:
        - Controls whether the role manages `telegraf.conf`.
        required: false
        type: bool
      telegraf_manage_plugin_configs:
        description:
        - Controls whether the role manages files in `telegraf.d`.
        required: false
        type: bool
      telegraf_apt_repo:
        description:
        - Controls `telegraf_apt_repo` for the `telegraf` role.
        required: false
        type: str
      telegraf_yum_repo_baseurl:
        description:
        - Controls `telegraf_yum_repo_baseurl` for the `telegraf` role.
        required: false
        type: str
      telegraf_gpg_key_url:
        description:
        - Controls `telegraf_gpg_key_url` for the `telegraf` role.
        required: false
        type: str
      telegraf_config_path:
        description:
        - Controls `telegraf_config_path` for the `telegraf` role.
        required: false
        type: str
      telegraf_plugins_path:
        description:
        - Controls `telegraf_plugins_path` for the `telegraf` role.
        required: false
        type: str
      telegraf_service_name:
        description:
        - Controls `telegraf_service_name` for the `telegraf` role.
        required: false
        type: str
      telegraf_package_name:
        description:
        - Controls `telegraf_package_name` for the `telegraf` role.
        required: false
        type: str
      telegraf_plugins:
        description:
        - Legacy structured plugin snippets rendered into `telegraf.d`.
        required: false
        type: list
      telegraf_plugins_group_vars:
        description:
        - Group-level legacy plugin snippets rendered into `telegraf.d`.
        required: false
        type: list
      telegraf_plugins_host_vars:
        description:
        - Host-level legacy plugin snippets rendered into `telegraf.d`.
        required: false
        type: list
      telegraf_plugins_playbook:
        description:
        - Playbook-level legacy plugin snippets rendered into `telegraf.d`.
        required: false
        type: list
      telegraf_plugin_files:
        description:
        - Preferred snippet interface for files in `telegraf.d`.
        required: false
        type: list
      telegraf_plugin_files_group_vars:
        description:
        - Group-level `telegraf.d` snippet files.
        required: false
        type: list
      telegraf_plugin_files_host_vars:
        description:
        - Host-level `telegraf.d` snippet files.
        required: false
        type: list
      telegraf_plugin_files_playbook:
        description:
        - Playbook-level `telegraf.d` snippet files.
        required: false
        type: list
      telegraf_global_tags:
        description:
        - Base global tags rendered into `telegraf.conf`.
        required: false
        type: dict
      telegraf_global_tags_group_vars:
        description:
        - Group-level global tags rendered into `telegraf.conf`.
        required: false
        type: dict
      telegraf_global_tags_host_vars:
        description:
        - Host-level global tags rendered into `telegraf.conf`.
        required: false
        type: dict
      telegraf_global_tags_playbook:
        description:
        - Playbook-level global tags rendered into `telegraf.conf`.
        required: false
        type: dict
      telegraf_config:
        description:
        - Controls `telegraf_config` for the `telegraf` role.
        required: false
        type: dict
