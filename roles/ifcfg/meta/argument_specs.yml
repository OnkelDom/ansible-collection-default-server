---
argument_specs:
  main:
    short_description: Entry point for the ifcfg role.
    description:
    - Validate variables for the `ifcfg` role.
    options:
      ifcfg_interfaces:
        description:
        - Controls `ifcfg_interfaces` for the `ifcfg` role.
        required: false
        type: list
        elements: dict
