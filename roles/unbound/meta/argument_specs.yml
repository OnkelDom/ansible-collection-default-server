---
argument_specs:
  main:
    short_description: Entry point for the unbound role.
    description:
    - Validate variables for the `unbound` role.
    options:
      unbound_use_blocklist:
        description:
        - Controls `unbound_use_blocklist` for the `unbound` role.
        required: false
        type: bool
      unbound_blocklist_url:
        description:
        - Controls `unbound_blocklist_url` for the `unbound` role.
        required: false
        type: str
      unbound_manage_service:
        description:
        - Controls `unbound_manage_service` for the `unbound` role.
        required: false
        type: bool
      unbound_service_enabled:
        description:
        - Controls `unbound_service_enabled` for the `unbound` role.
        required: false
        type: bool
      unbound_service_state:
        description:
        - Controls `unbound_service_state` for the `unbound` role.
        required: false
        type: str
      unbound_manage_firewall:
        description:
        - Controls `unbound_manage_firewall` for the `unbound` role.
        required: false
        type: bool
      unbound_firewall_zone:
        description:
        - Controls `unbound_firewall_zone` for the `unbound` role.
        required: false
        type: str
      unbound_manage_selinux:
        description:
        - Controls `unbound_manage_selinux` for the `unbound` role.
        required: false
        type: bool
      unbound_zones_dir:
        description:
        - Controls `unbound_zones_dir` for the `unbound` role.
        required: false
        type: str
      unbound_local_zones:
        description:
        - Controls `unbound_local_zones` for the `unbound` role.
        required: false
        type: list
      unbound_stub_zones:
        description:
        - Controls `unbound_stub_zones` for the `unbound` role.
        required: false
        type: list
      unbound_forward_zones:
        description:
        - Controls `unbound_forward_zones` for the `unbound` role.
        required: false
        type: list
      unbound_auth_zones:
        description:
        - Controls `unbound_auth_zones` for the `unbound` role.
        required: false
        type: list
      unbound_config:
        description:
        - Controls `unbound_config` for the `unbound` role.
        required: false
        type: dict
